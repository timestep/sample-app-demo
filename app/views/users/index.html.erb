<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |s| %>
    <li>
      <%= gravatar_for s, size: 52 %>
      <%= link_to s.name, s %>
    </li>
  <% end %>
</ul>